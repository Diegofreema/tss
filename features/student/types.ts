export type StudentSuccessResponseType = {
  success: boolean;
  message: string;
  data: [];
};

export type StudentType = {
  regnum: string;
  fname: string;
  lname: string;
  classname: string;
};
